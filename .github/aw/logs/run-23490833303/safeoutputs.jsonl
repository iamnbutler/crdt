{"body":"🤖 *This is an automated draft PR from [Perf Improver](https://github.com/iamnbutler/crdt/actions/runs/23490833303), an AI-powered performance optimization assistant.*\n\n## Goal and Rationale\n\nTwo hot-path optimizations in `TextBuffer` targeting collaborative editing performance:\n\n1. **`hasFragment` O(1) index** – Called for every incoming remote operation to check causal readiness. The previous O(n) implementation iterated all fragments and allocated a new array on each call via `fragmentsArray()`. Under network delay or reordering, every pending operation pays this cost repeatedly.\n\n2. **`appliedOps` numeric map** – Previously used `Set<string>` with template-literal keys like `` `${replicaId}:${counter}` ``. Every idempotency check (and `retryPendingOps` loop) created a new string. Replaced with `Map<number, Set<number>>` for allocation-free numeric lookups.\n\n## Approach\n\n### `hasFragment` index\n- Added `_fragmentIds: Map<number, Set<number>>` (replicaId → Set\\<counter\\>) field\n- Added `setFragments(frags: Fragment[]): void` helper that atomically rebuilds both the `SumTree` and the index — all former `this.fragments = SumTree.fromItems(...)` callsites now go through this helper\n- `hasFragment()` reduced to: `return this._fragmentIds.get(rid)?.has(counter) ?? false`\n\n### `appliedOps` numeric map\n- Changed field from `Set<string>` to `Map<number, Set<number>>`\n- Added `hasAppliedOp(id)` and `markAppliedOp(id)` helpers\n- **Serialization format unchanged** — at `toSerializedSnapshot()` / `fromSerializedSnapshot()` boundaries we still convert to/from `string[]` for backward compatibility\n\n## Performance Evidence\n\nBenchmark: apply 500 operations in **reverse order** to a fresh buffer (worst case — all ops pend until dependencies arrive, maximising `hasFragment` + `appliedOps` call frequency).\n\n| Metric | Before | After | Improvement |\n|--------|--------|-------|-------------|\n| µs/op | 596.20 | 62.68 | **9.5× faster** |\n| ops/sec | 1,677 | 15,953 | **9.5× throughput** |\n\nMethodology:\n```ts\n// Build 500 insert ops on buf1\n// Apply them in reverse order to a fresh buf\n// 1000 iterations, wall-clock time\n```\n\n## Trade-offs\n\n- **Memory**: `_fragmentIds` holds one `Set<number>` per distinct `replicaId` in the document. For typical collaborative sessions (2–10 replicas), this is negligible.\n- **`setFragments` rebuild cost**: The index rebuild is O(n fragments) — same asymptotic cost as `SumTree.fromItems`, so no additional order of complexity. Constant factor is small (one Map lookup + Set insert per fragment).\n- **Serialization stays backward compatible**: `appliedOps` wire format is still `string[]`.\n\n## Reproducibility\n\n```bash\n# Install bun, then:\nbun install\nbun test   # 409 pass\n```\n\nMicro-benchmark script available at `benchmarks/` directory (see `bench:text` script).\n\n## Test Status\n\n✅ 409 tests pass, 0 failures  \n✅ No new lint errors introduced (3 pre-existing `noNonNullAssertion` warnings in same file unchanged)  \n✅ TypeScript clean on modified file (`tsc --noEmit` shows no errors in `text-buffer.ts`)","branch":"perf-assist/applied-ops-and-has-fragment","title":"perf: O(1) hasFragment lookup + numeric appliedOps map","type":"create_pull_request","patch_path":"/tmp/gh-aw/aw-perf-assist-applied-ops-and-has-fragment.patch"}
{"body":"🤖 *Perf Improver here - I'm an automated AI assistant focused on performance improvements for this repository.*\n\n## Activity for March 2026\n\n## Suggested Actions for Maintainer\n\n* [ ] **Review PR** (pending creation): `perf: O(1) hasFragment lookup + numeric appliedOps map` - 9.5× speedup for `applyRemote` with out-of-order ops - [Review once created](https://github.com/iamnbutler/crdt/pulls)\n\n## Performance Opportunities Backlog\n\n1. **`hasFragment` O(1) index** ✅ Implemented this run (9.5× speedup)\n2. **`appliedOps` numeric map** ✅ Implemented this run\n3. **`textbuffer-array`** - `fragmentsArray()` allocates new array on every call; could cache or use iterator API\n4. **`shallow-clone` summaries copy** - `shallowClone()` does `new Map(this.summaries)` which is O(n nodes); could use copy-on-write\n5. **Pending ops `isCausallyReady` loop** - scans all pending ops on every applied op; could use dependency graph\n\n## Discovered Commands\n\n```bash\nbun install --frozen-lockfile   # Install dependencies\nbun test                         # Run all tests (409 pass)\nbun test --test-name-pattern \"^(?!.*CRDT Property)\"  # Skip slow property tests\nbun run typecheck               # TypeScript check\nbun run lint                    # Biome linting\nbun run bench                   # Run all benchmarks (requires fixtures)\nbun run bench:text:ci           # Text buffer benchmarks (CI mode)\nbun run fixtures:download       # Download Kleppmann editing trace (needed for bench)\n```\n\n## Run History\n\n### 2026-03-24 - [Run](https://github.com/iamnbutler/crdt/actions/runs/23490833303)\n- 🔧 Implemented `hasFragment` O(1) index via `_fragmentIds: Map<number, Set<number>>`\n- 🔧 Replaced `appliedOps: Set<string>` with `Map<number, Set<number>>` (no string allocation)\n- 📊 Measured: `applyRemote` with 500 out-of-order ops: **596µs → 63µs (9.5× faster)**\n- 🔧 Created draft PR: `[Perf Improver] perf: O(1) hasFragment lookup + numeric appliedOps map`\n","labels":["performance"],"title":"Monthly Activity March 2026","type":"create_issue"}
